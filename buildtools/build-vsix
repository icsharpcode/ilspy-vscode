#!/usr/bin/env sh

PROJECTVERSION=`cat .projectversion`

mkdir ./artifacts

cd ./vscode-extension
npm install
npm run package
npm install @vscode/vsce -g
vsce package -o ../artifacts/ilspy-vscode-$PROJECTVERSION.vsix
